SCOPE
ES6 Block level scope
Two new declarations LET and CONST
Lookup var vs let
Lookup strict vs non sctrict
Lookup whether it's a good idea to use var over let
Scope is what's visible between a boundary
Play around with scope between blocks

CONTEXT
Lookup 'THIS'
Create new person
var name = paul
person.hello.call(window);

* scope
* this
* using call with different arguments
* trick to turn arguments into a legitimate array 
	array.prototype.slice.call
* fat arrow syntax

bespoke.js